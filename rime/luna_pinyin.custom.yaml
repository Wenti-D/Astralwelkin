# luna_pinyin.custom.yaml
#
# 补丁功能：
#   自定义标点符号
#
# 作者：Windy_D <windy0deng@gmail.com>
#
# 放入 RIME 的用户文件夹，重新部署即刻生效。
# 建议配合作者制作的主题食用。
#
#
# 注：
#   本补丁适用于朙月拼音。
#

patch:
  punctuator:
    full_shape:
      ' ': { commit: '　' }
      ',': { commit: ， }
      '.': { commit: '.' }
      '。': { commit: 。 }
      '<': ＜
      '>': ＞
      '/': ／
      '|': ｜
      '\': ＼
      '`' : ｀
      '~' : ～
      '@': ＠
      '#': ＃
      '%': ％
      '$': '$'
      '^' : { commit: …… }
      '&' : ＆
      '*': ＊
      '(' : （
      ')' : ）
      '-' : －
      '_' : ——
      '+' : ＋
      '=' : ＝
      '[': 【
      ']': 】
      '{': ｛
      '}': ｝

    half_shape:
      ',': { commit: ， }
      '.': { commit: 。 }
      '<': '<'
      '>': '>'
      '?': { commit: ？ }
      ';': { commit: ； }
      ':': { commit: ： }
      '''': { pair: [ '‘', '’' ] }
      '"': { pair: [ '“', '”' ] }
      '\': '\'
      '|': '|'
      '`': '`'
      '~': '~'
      '!': { commit: ！ }
      '@': '@'
      '#': '#'
      '%': '%'
      '$': '$'
      '^': '^'
      '&': '&'
      '*': '*'
      '(': '('
      ')': ')'
      '_': '_'
      '[': '['
      ']': ']'
      '{': '{'
      '}': '}'
